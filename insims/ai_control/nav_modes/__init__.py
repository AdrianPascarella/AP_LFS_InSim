from abc import ABC

class AINavModeState(ABC):
    """
    Clase base para los modos de navegación de la IA.
    Cada modo (Ruta o Freeroam) hereda de esta clase y almacena
    solo las variables que necesita.
    """
    pass
